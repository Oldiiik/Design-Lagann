# GitHub Sponsors is intentionally inactive until the official account is live.
# To activate the repository Sponsor button, uncomment and replace the handle:
# github: [your-github-sponsors-handle]

# Optional: add up to four approved funding URLs after they are public.
# custom: ["https://your-domain.example/support"]

